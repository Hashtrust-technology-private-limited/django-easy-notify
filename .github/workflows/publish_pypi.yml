name: Publish Python 🐍 distributions 📦 to PyPI

on:
  push:
    branches:
      - test-publish-action

jobs:
  build-n-publish:
    name: Build and publish Python 🐍 distributions 📦 to PyPI
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: Set up Python 3.10
      uses: actions/setup-python@v3
      with:
        python-version: '3.10'
    - name: "Installs and upgrades pip, installs other dependencies"
      run: |
        python3 -m pip install --upgrade pip
        python3 -m pip install setuptools wheel twine bumpversion
        pip install -r requirements.txt

    - name: Bump version
      run: |
        CURRENT_VERSION=$(python setup.py --version)
        NEW_VERSION=$(bumpversion --list patch --allow-dirty | grep new_version | cut -d= -f2)
        export PACKAGE_VERSION=${NEW_VERSION}
        echo "::set-output name=PACKAGE_VERSION::$PACKAGE_VERSION"
      id: bump_version

    - name: installs the package from setup.py
      run: |
        python3 setup.py install

    - name: Set env
      run: echo "VERSION_PLACEHOLDER=${{ steps.bump_version.outputs.PACKAGE_VERSION }}" >> $GITHUB_ENV

    - name: Build and Upload to TestPyPI
      run: |
        python3 setup.py sdist bdist_wheel
        python3 -m twine upload dist/*
      env:
        TWINE_USERNAME: __token__
        TWINE_PASSWORD: pypi-AgENdGVzdC5weXBpLm9yZwIkZTEzNzcxYjEtZTdkMS00ZjFjLTg4ODYtYmE2NzZmZWM0NmRhAAIqWzMsIjE3YzFkZTk2LWU2NzctNDkwMS1iMmE2LTZkODJhMDcyNzY3OCJdAAAGIPo03-wh2w_E3-NbGXt9OV_wyydq_wq87R1igq4C7V1u
        TWINE_REPOSITORY: testpypi
